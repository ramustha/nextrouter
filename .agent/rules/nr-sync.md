---
description: Sync NextRouter rules across all active providers
---

Please call the NextRouter MCP tool `sync_rules` immediately (with `workspacePath` pointing to the current workspace directory) or run the command `nextrouter sync` or `npm run cli sync` in the terminal if the MCP tool is not available. Do not ask for confirmation.
