---
description: Show NextRouter active providers and sessions status
---

Please call the NextRouter MCP tool `get_shared_context` immediately (or run the command `nextrouter status` or `npm run cli status` in the terminal if the MCP tool is not available) and print the output. Do not ask for confirmation.
